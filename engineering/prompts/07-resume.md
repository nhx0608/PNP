# 任务：从仓库恢复指定工作包

读取根 AGENTS.md、工作包表、当前分支、git status、最近模块交付记录和公共契约版本。根据分支所有者识别 A/B/C；无法确定时只询问工作包编号，不猜测其他角色任务。

确认公共基线与依赖锁，列出已实现入口、未满足验收项和真实测试证据。以仓库文件为依据，不依赖聊天摘要。仅继续本角色允许目录，公共变更独立申请。

一次完成一个可测试的行为单元，运行对应测试并更新模块交付记录。不得把测试未执行写成通过，不修改他人未提交内容，不擅自推送。
